---
description: Firebase Analytics client + leftover legacy Cloud Functions. Assistant/weekly on hosting-php; license deleted.
globs:
  - functions/**
  - extensions/**
  - firebase.json
  - firestore.rules
  - firestore.indexes.json
alwaysApply: false
---

# Firebase backend (remaining)

Skill: [`.cursor/skills/firebase-backend/SKILL.md`](../skills/firebase-backend/SKILL.md)

**On hosting-php (`api.carlmanuel.com`):** contact, quotation, trackVisit, previewFeedback, analyticsSummary, **assistant**, weekly visit report cron. **license** deleted.

## Still Firebase

| Piece | Purpose |
|--------|---------|
| Analytics client (`src/config.js`) | `logEvent` page/chat metrics |
| Optional legacy handlers in `functions/index.js` | Unused by prod mapping; emulator / rollback only |

## `src/mapping.js`

All forms / analytics / **assistant** → `https://api.carlmanuel.com/...`

## Deploy (legacy functions only)

**Gate — tests first:**

```bash
cd functions && npm test
```

Must pass before:

```bash
cd functions && npm run deploy
```

Assistant / weekly report / forms: skill **api-carlxaeron** + **hosting-ssh** (not this deploy).
