rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Portfolio data is written only via Cloud Functions (Admin SDK).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
